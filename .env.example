# Copy this file to .env and fill in real values, OR export these as
# shell environment variables. Never commit real credentials.
# .env is already excluded via .gitignore.

NET_USERNAME=admin
NET_PASSWORD=changeme
NET_SECRET=
